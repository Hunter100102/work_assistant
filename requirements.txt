fastapi==0.112.*
uvicorn[standard]==0.30.*   # includes uvloop + httptools (faster, low overhead)
gunicorn==22.*
httpx==0.27.*
python-dotenv==1.0.*        # for local dev only; Render uses env vars
pydantic==2.8.*             # FastAPI models
